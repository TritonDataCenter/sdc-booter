#!/usr/bin/env node

var mapi = require('../mapi');

var mac = process.argv[2];
if (!mac) {
  console.log("Usage: " + process.argv[1] + " mac_addr");
  process.exit(1);
}

mapi.getBootParams(mac, function(params, err) {
  if (err) {
    console.log(err);
  } else {
    console.log(params);
  }
});
